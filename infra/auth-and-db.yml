AWSTemplateFormatVersion: '2010-09-09'
Description: Movie Watchlist - DynamoDB table + Cognito user pool

Parameters:
  TableName:
    Type: String
    Default: MovieWatchlist
    Description: DynamoDB table name

  CognitoDomainPrefix:
    Type: String
    Default: movie-watchlist-auth
    Description: Must be globally unique in the AWS region (for Hosted UI)

  CallbackUrl:
    Type: String
    Default: http://localhost:5173/
    Description: SPA callback URL (for dev / local)

  LogoutUrl:
    Type: String
    Default: http://localhost:5173/
    Description: SPA logout URL (for dev / local)

Resources:

  ##########################################
  # DynamoDB Table
  ##########################################
  WatchlistTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref TableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: itemId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: itemId
          KeyType: RANGE

  ##########################################
  # Cognito User Pool
  ##########################################
  CognitoUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: movie-watchlist-user-pool
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      Schema:
        - Name: email
          Required: true
          Mutable: true
          AttributeDataType: String

  ##########################################
  # Cognito SPA App Client
  ##########################################
  CognitoUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref CognitoUserPool
      ClientName: movie-watchlist-spa
      GenerateSecret: false
      AllowedOAuthFlowsUserPoolClient: true
      AllowedOAuthFlows:
        - code
      AllowedOAuthScopes:
        - openid
        - email
        - profile
      SupportedIdentityProviders:
        - COGNITO
      CallbackURLs:
        - !Ref CallbackUrl
      LogoutURLs:
        - !Ref LogoutUrl
      PreventUserExistenceErrors: ENABLED

  ##########################################
  # Cognito Hosted UI Domain
  ##########################################
  CognitoUserPoolDomain:
    Type: AWS::Cognito::UserPoolDomain
    Properties:
      Domain: !Ref CognitoDomainPrefix
      UserPoolId: !Ref CognitoUserPool

Outputs:

  DynamoTableName:
    Description: DynamoDB Table Name
    Value: !Ref WatchlistTable

  DynamoTableArn:
    Description: DynamoDB Table ARN
    Value: !GetAtt WatchlistTable.Arn

  CognitoUserPoolId:
    Description: Cognito User Pool ID
    Value: !Ref CognitoUserPool

  CognitoUserPoolArn:
    Description: Cognito User Pool ARN
    Value: !GetAtt CognitoUserPool.Arn

  CognitoUserPoolClientId:
    Description: Cognito SPA Client ID
    Value: !Ref CognitoUserPoolClient

  CognitoDomainUrl:
    Description: Hosted UI Login URL
    Value: !Sub "https://${CognitoDomainPrefix}.auth.${AWS::Region}.amazoncognito.com"

  Region:
    Description: Deployment region
    Value: !Ref AWS::Region